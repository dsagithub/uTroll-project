<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.eetac.dsa.dsaqt1415g4.utroll" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".uTrollMainActivity" />
        <activity android:name=".GroupListActivity" />
        <activity android:name=".GroupDetailActivity" />
        <activity android:name=".CommentDetailActivity" />
        <activity android:name=".WriteCommentActivity" />
        <activity android:name=".WriteCommentTrollActivity" />
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".UserSearchActivity" />
        <activity android:name=".UserDetailActivity" />
        <activity android:name=".PendingFriendsActivity" />
    </application>

</manifest>
